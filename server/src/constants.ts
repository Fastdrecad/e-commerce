export enum EMAIL_PROVIDER {
  Email = "Email",
  Google = "Google",
  Facebook = "Facebook",
  Apple = "Apple"
}

export enum ROLES {
  Admin = "Admin",
  Member = "Member"
}

export enum TOKEN_TYPES {
  ACCESS = "access",
  REFRESH = "refresh"
}
